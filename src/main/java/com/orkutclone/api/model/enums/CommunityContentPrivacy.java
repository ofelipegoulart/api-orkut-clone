package com.orkutclone.api.model.enums;

public enum CommunityContentPrivacy {
    /** Content is visible to anyone, even non-members. */
    OPEN_TO_NON_MEMBERS,
    /** Content is visible only to members. */
    RESTRICTED
}
