package com.orkutclone.api.model.enums;

public enum CommunityType {
    /** Anyone can join without approval. */
    PUBLIC,
    /** The owner must approve each join request before the user becomes a member. */
    MODERATED
}
